@model IEnumerable<UserSupport>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div calss="row p-4">
        <div class="col-6">
            <h2 class="text-primary">Мої звернення </h2>
        </div>
        @*<div class="text-end">
            <a asp-controller="Product" asp-action="Create" class="btn btn-primary">
                Створити новий продукт
            </a>
        </div>*@
    </div>
</div> 

<div class="container">
    @foreach(var message in Model)
    {
         @*<textarea disabled class="form-control z-depth-1" id="exampleFormControlTextarea6" rows="3" placeholder="@message.TextMessage"></textarea>*@ 
        <div class="card border-primary mb-3" style="max-width: 100%;">
                <div class="card-body">
                    <p class="card-text">@message.TextMessage</p>
                </div>
        </div>
         <br />
    }
</div>
